import { trailRating } from '../../../helpers/trail-rating';
import { module, test } from 'qunit';

module('Unit | Helper | trail rating');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = trailRating(42);
  assert.ok(result);
});
